
<li class="nav-item dropdown">
    <a class="nav-link" data-toggle="dropdown" href="#">
        <i class="far fa-bell"></i>
    </a>


<div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
    <span class="dropdown-item dropdown-header">New Notifications</span>
    {% for notif in notifications %}
    <div class="dropdown-divider"></div>
    <a href="#" class="dropdown-item">
        <i class="fas fa-file mr-2"></i> {{ notif.title }}
        <span class="float-right text-muted text-sm">{{ notif.date|date }}</span>
    </a>
    <div class="dropdown-divider"></div>
    {% endfor %}
    <a href="{{ path('notif_list' )}}" class="dropdown-item dropdown-footer">See All Notifications</a>
</div>

</li>


{% extends  'back.html.twig' %}
{% block contenu %}

    <h1>Eventcour edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
          {#  <a href="{{ path('eventcours_index') }}">Back to the list</a> #}
        </li>

    </ul>
{% endblock %}